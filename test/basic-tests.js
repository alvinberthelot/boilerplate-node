var chai = require('chai');
 // we are using the "expect" style of Chai
var expect = chai.expect;

describe('Basic tests', function() {

  it('true is true (of course)', function() {
    expect(true).to.be.true;
  });

});
